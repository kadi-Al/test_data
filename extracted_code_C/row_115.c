#include <stdio.h>

int main() {
    int x;  // Declaration of an integer variable without initialization

    // Assign a value to x, here we use 42 as an example
    x = 42;

    printf("%d\n", x);  // Printing the value of x

    return 0;
}